<Page
    x:Class="Bangumi.Views.ProgressPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bangumi.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:Bangumi.Models"
    xmlns:common="using:Bangumi.Common"
    SizeChanged="Page_SizeChanged"
    NavigationCacheMode="Enabled"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <DataTemplate x:Key="ProgressSubjectTemplate" x:DataType="data:WatchingStatus">
            <Grid Padding="5"
                  Width="{Binding ElementName=MyWidth,Path=Width}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image Source="{x:Bind image}" 
                        Height="75" 
                        Width="75" 
                        Stretch="UniformToFill" />
                <Grid Grid.Column="1"
                        Padding="5,0,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="{x:Bind name_cn}" 
                               FontSize="14" 
                               VerticalAlignment="Center"
                               Foreground="#d26585"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Grid.Row="1"
                               Text="{x:Bind name}" 
                               VerticalAlignment="Center"
                               FontSize="12" 
                               Foreground="Gray"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Grid.Row="2"
                               FontSize="12" 
                               VerticalAlignment="Center"
                               Foreground="{x:Bind ep_color, Mode=OneWay}"
                               TextTrimming="CharacterEllipsis">
                        <Run Text="{x:Bind watched_eps, Mode=OneWay}"/>
                        <Run Text="/"/>
                        <Run Text="{x:Bind eps_count, Mode=OneWay}"/>
                    </TextBlock>
                    <Grid Grid.Row="3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <Button Grid.Column="3"
                                Click="NextEpButton_Click"
                                Padding="8,3"
                                VerticalAlignment="Bottom"
                                BorderThickness="1"
                                Background="{ThemeResource AppBarItemBackgroundThemeBrush}"
                                Style="{StaticResource ButtonRevealStyle}">
                                <TextBlock FontSize="12" 
                                           Foreground="{x:Bind ep_color, Mode=OneWay}"
                                           VerticalAlignment="Center">
                                    <Run Text="EP."/>
                                    <Run Text="{x:Bind next_ep, Mode=OneWay}"/>
                                </TextBlock>
                        </Button>
                    </Grid>
                    <ProgressRing IsActive="True"
                                  Visibility="{x:Bind isUpdating,Mode=OneWay}"
                                  Grid.RowSpan="4"
                                  VerticalAlignment="Top"
                                  HorizontalAlignment="Right"
                                  Width="20" 
                                  Height="20" >
                        <ProgressRing.RenderTransform>
                            <CompositeTransform CenterX="10"
                                                CenterY="10"
                                                ScaleX="0.6" 
                                                ScaleY="0.6"/>
                        </ProgressRing.RenderTransform>
                    </ProgressRing>

                </Grid>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <GridView Name="MyGridView" 
                  Padding="12,6,12,46"
                  ItemsSource="{x:Bind ViewModel.watchingCollection}" 
                  IsItemClickEnabled="True" 
                  ItemClick="GridView_ItemClick"
                  ItemTemplate="{StaticResource ProgressSubjectTemplate}">
        </GridView>
        
        <Border Name="MyWidth"
                Visibility="Collapsed"/>

    </Grid>
</Page>
